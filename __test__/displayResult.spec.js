import { displayResult } from "../src/client/js/displayResult";

describe("Testing the display result functionality", () => {
  test("should have a defined function", () => {
    expect(displayResult).toBeDefined();
  });
});
